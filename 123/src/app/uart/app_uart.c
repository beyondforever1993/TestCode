/*文件名称: app_uart.c功能:    1.包含Uart数据解析相关的函数作者: 杜在连修改记录:    2017-4-1 文件初创备注: void注意:    1.由于现有的串口协议大多以2Bytes字符串做开头/结尾，      故ISR中只接收以2Bytes字符串开头/结尾的数据包,且最大包长不得大于100Bytes。如有变更可更改UartSaveData()函数*/#include "include.h"/*名称: UartPrintBuffer()功能:    1.将缓存中的数据全部打印输出参数:       1.pData:        指向待打印的数据    2.usDataLen:    待打印的数据长度返回值: void输入:   void输出:   void备注:   void注意:   void*/void UartPrintBuffer(uint8_t *pData, uint16_t usDataLen){        uint16_t i = 0;            while(usDataLen--)        {                printf("0x%02x ", pData[i]);                i++;                if ((i % 10) == 0)                {                        printf("\r\n");                }        }        printf("\r\n");    return;}#if TEST_EN/*名称: UartDeal()功能:    1.将缓存中的数据全部打印输出参数:       1.pData:        指向待打印的数据    2.usDataLen:    待打印的数据长度返回值: void输入:   void输出:   void备注:   void注意:   void*/void UartDeal(uint8_t *pData, uint16_t usDataLen){        return;}#endif