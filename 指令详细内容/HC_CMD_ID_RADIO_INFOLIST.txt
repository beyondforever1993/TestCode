reply:len = 26byte
get:len = 0
取值范围


typedef enum
{
    RADIO_MODULE_TYPE__START = 0,
    RADIO_MODULE_TYPE__NONE,
    RADIO_MODULE_TYPE__HC_RX,
    RADIO_MODULE_TYPE__HC_RXTX,
    RADIO_MODULE_TYPE__LFS,
    RADIO_MODULE_TYPE__SATEL,
    RADIO_MODULE_TYPE__LFS_LOW,
    RADIO_MODULE_TYPE__HX_FULL,
    RADIO_MODULE_TYPE__END,
} RADIO_MODULE_TYPE_ENUM;
typedef enum
{
    RADIO_FREQUENCY_TYPE__START = 0,
    RADIO_FREQUENCY_TYPE__HIGH,
    RADIO_FREQUENCY_TYPE__MIDDLE,
    RADIO_FREQUENCY_TYPE__LOW,
    RADIO_FREQUENCY_TYPE__ALL,
    RADIO_FREQUENCY_TYPE__END,
} RADIO_FREQUENCY_TYPE_ENUM;
typedef struct
{
    HC_UINT16         module_type;//RADIO_MODULE_TYPE_ENUM
    HC_UINT16         frq_type;//RADIO_FREQUENCY_TYPE_ENUM
    HC_UINT32         frq_start;   //not used
    HC_UINT32         frq_end;    //not used
    HC_UINT16         stepper;//RADIO_STEPPER_ENUM //not used
    
    HC_UINT32         air_baud_support_list;//the index is RADIO_AIR_BAUD_ENUM按位取值，从最低位开始0代表不支持 //not used
    HC_UINT32         protocol_support_list;//the index is RADIO_PROTOCOL_ENUM同上 //not used
    HC_UINT32         power_support_list;//the index is RADIO_POWER_ENUM同上 //not used
    
} HC_HW_INFO_OF_RADIO_STRUCT;


注：此命令已经无效，返回HC_CMD_ID_RADIO_MODULE_TYPE