get:len = 0byte
reply:len = 36byte
取值范围

typedef struct
{
    HC_UINT32 machine_type; //HC_PRODUCT_MODEL_ENUM
    HC_INT8   sn[24];
    HC_INT8   pn[24];
    HC_UINT32 radio_type;
    HC_UINT32 gprs_type;  //HC_GPRS_MODEL_ENUM
    HC_UINT32 bt_type;
    HC_UINT32 gsensor_type; //HC_GSENSOR_MODEL_ENUM
} HARDWARE_INFO_STRUCT;
//修改sn号为7位，第一位为R，后面跟数字


typedef enum _product_type
{
	HC_PRODUCT_MODEL__UNKNOW,
	HC_PRODUCT_MODEL__N72,
	HC_PRODUCT_MODEL__I80
} HC_PRODUCT_MODEL_ENUM;

typedef enum _product_type
{
	HC_GSENSOR_MODEL__NULL,
	HC_GSENSOR_MODEL__LIS3DH,
	HC_GSENSOR_MODEL__HUAYING,
	HC_GSENSOR_MODEL__HUAXIN
} HC_GSENSOR_MODEL_ENUM;


typedef enum _product_type
{
	HC_GPRS_MODEL__NULL,
	HC_GPRS_MODEL__CE910, //不支持csd
	HC_GPRS_MODEL__UL865, 
	HC_GPRS_MODEL__HE910
} HC_GPRS_MODEL_ENUM;